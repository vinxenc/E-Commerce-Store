
[phases.setup]
nixPkgs = ['nodejs', 'yarn']

[phases.install]
cmds = ['yarn install']

[phases.buildfrontend]
cmds = ['yarn run frontend:build']

[phases.buildbackend]
cmds = ['yarn run backend:build']
dependsOn = ['buildfrontend']

[start]
cmd = 'yarn run start'
