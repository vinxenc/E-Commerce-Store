
[phases.setup]
nixPkgs = ['nodejs', 'yarn']

[phases.install]
cmds = ['yarn install']

[phases.build]
cmds = ['yarn run build']

[phase.copy]
cmds = ['cp -v packages/frontend/public packages/backend']

[start]
cmd = 'yarn run start'